services:
  - type: web
    name: hospital-duty-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        value: встав_сюди_свій_токен_від_BotFather
